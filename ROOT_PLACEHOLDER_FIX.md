# اصلاح نمایش `root` در صفحهٔ Subscription

## علت

`root` نام حساب یا شناسهٔ Subscription نبود. این مقدار در دادهٔ لینک، داخل فیلد یادداشت (`note`) ذخیره شده بود و صفحهٔ عمومی آن را در کارت اطلاع‌رسانی بالای جدول اطلاعات نمایش می‌داد.

## اصلاح

تابع `public_announcement` پیش از ساخت مدل دادهٔ عمومی اجرا می‌شود. اگر مقدار یادداشت خالی یا یکی از placeholderهای سیستمی زیر باشد، به مرورگر ارائه نمی‌شود:

- `root`
- `admin`
- `administrator`
- `rvg`

یادداشت‌های واقعی واردشده توسط اپراتور بدون تغییر نمایش داده می‌شوند. نام حساب در جدول همچنان فقط از نام سفارشی کانفیگ یا هش ۷ حرفی ساخته می‌شود.

## پوشش آزمون

آزمون یکپارچه تأیید می‌کند که `note=root` در پاسخ API و bootstrap صفحه به رشتهٔ خالی تبدیل می‌شود، ولی یک یادداشت واقعی مانند پیام زمان نگه‌داری نمایش داده خواهد شد.
