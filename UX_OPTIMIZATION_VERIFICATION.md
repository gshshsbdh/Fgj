# UX and instant-language verification

## اصلاح تغییر زبان

تغییر زبان دیگر به Refresh وابسته نیست. علت اصلی، تلاش `applyPreference()` برای نوشتن در عنصر loading حذف‌شده پس از رندر اولیه بود که اجرای handler تغییر زبان را متوقف می‌کرد. کنترل به‌روزرسانی اکنون فقط در صورت وجود عنصر موردنظر انجام می‌شود و همهٔ بخش‌های پویا با دادهٔ موجود دوباره رندر می‌شوند.

در تغییر زبان، `lang` و `dir` سند، عنوان صفحه، متن‌های جدول، وضعیت، زمان/تاریخ، متن Subscription، عملیات کپی، Android، پیام‌های خطا، فرم رمز، tooltipها، عنوان/alt QR و برچسب‌های دسترسی‌پذیری فوراً به‌روز می‌شوند. زبان و تم با storage ایمن، حتی در محیط‌هایی که localStorage در دسترس نیست، مدیریت می‌شوند.

## بهینه‌سازی UX و عملکرد

| بهینه‌سازی | نتیجه |
| --- | --- |
| Bootstrap data | در حالت معمول رندر اول از دادهٔ درون HTML انجام می‌شود و fetch اولیه حذف می‌شود. |
| Refresh دوره‌ای | هر ۶۰ ثانیه، فقط زمانی که tab قابل مشاهده است، به‌صورت silent انجام می‌شود. |
| Fingerprint داده | اگر داده تغییری نکرده باشد، صفحه دوباره render نمی‌شود و پرش بصری رخ نمی‌دهد. |
| Loading | loader فقط هنگام فقدان دادهٔ اولیه نمایش داده می‌شود؛ refreshهای بعدی بدون loader هستند. |
| درخواست شبکه | timeout هشت‌ثانیه‌ای و جلوگیری از درخواست هم‌زمان اعمال شده است. |
| فهرست کانفیگ‌ها | `content-visibility` برای کاهش هزینهٔ رندر فهرست‌های طولانی افزوده شده است. |

## کنترل نهایی

در پیش‌نمایش واقعی، تغییر زبان انگلیسی→فارسی و فارسی→انگلیسی بدون تغییر URL، بدون Refresh و بدون خطای console انجام شد. آزمون Subscription شامل مرورگر، `?view=raw`، feed کلاینت‌ها، مسیر گروهی و اشتراک رمزدار نیز موفق بوده است.
