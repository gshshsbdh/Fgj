# سیاست نام‌گذاری و URL عمومی Subscription

## نام پیش‌فرض کاربر و کانفیگ

هنگام ایجاد کانفیگ یا گروه Subscription بدون نام، سیستم یک شناسهٔ قطعی **هفت‌حرفی انگلیسی و کوچک** می‌سازد. این شناسه از هش شناسهٔ همان رکورد تولید می‌شود تا پایدار باشد. در فرم ایجاد کانفیگ نیز یک مقدار هفت‌حرفی قابل ویرایش از ابتدا در فیلد نام نمایش داده می‌شود.

اپراتور می‌تواند نام پیش‌فرض را پیش از ساخت یا بعداً از طریق ویرایش به هر نام دلخواه تغییر دهد. اگر عنوان در ویرایش پاک شود، سیستم به همان نام هش‌دار پایدار بازمی‌گردد؛ بنابراین خروجی کلاینت هرگز نام خالی یا نام عمومی نمایش نمی‌دهد.

## خروجی قابل مشاهده برای کاربر

همهٔ URIهای محلی VLESS، Trojan، Shadowsocks و MTProto از عنوان پاک‌سازی‌شدهٔ کاربر/کانفیگ در fragment استفاده می‌کنند و پیشوند `RVG-` ندارند. داده‌های قدیمی با عنوان‌هایی مانند `RVG-...` نیز در زمان ارائه به feed به عنوان پاک‌سازی‌شده بازنویسی می‌شوند؛ دادهٔ اتصال و پارامترهای URI تغییر نمی‌کند.

## URL عمومی

هدر `support-url`، مدل دادهٔ `subSupportUrl` و ارجاع‌های عمومی قالب Subscription اکنون به URL واقعی همان اشتراک اشاره دارند:

- تک‌کاربره: `https://YOUR_DOMAIN/sub/UUID`
- گروهی: `https://YOUR_DOMAIN/sub-group/UUID_KEY`

هیچ ارجاع عمومی به `https://t.me/CodeBoxo` باقی نمانده است. URL feed خام همچنان با `?view=raw` برای Import در کلاینت‌ها ارائه می‌شود.

## آزمون‌ها

`test_naming_policy.py` هش هفت‌حرفی، نام سفارشی، پاک‌سازی داده‌های قدیمی، headerهای profile/support، feed تک‌کاربره و گروهی، همچنین داده‌های نود و خارجی را بررسی می‌کند. آزمون یکپارچهٔ Subscription نیز هم‌زمان اجرا شده است.

## قالب نام نمایشی در کلاینت‌های VPN

هر URI خروجی اکنون در fragment خود نامی با این قالب دارد:

```text
abcdefg|📊408.37MB|⌛35D
```

بخش اول، نام سفارشی کاربر یا هش هفت‌حرفی پیش‌فرض است. `📊` همیشه **حجم باقی‌مانده** را بدون فاصله نمایش می‌دهد و `⌛` همیشه **روزهای باقی‌مانده** را با `D` بزرگ نمایش می‌دهد. در سهمیه یا انقضای نامحدود، مقدار مربوطه `∞` است. این نام هر بار که کلاینت Subscription را Update می‌کند، از مصرف و انقضای فعلی بازتولید می‌شود.
