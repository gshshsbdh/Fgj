# اصلاح کپی URL در صفحهٔ Subscription

## رفتار جدید

دکمهٔ Copy و QR در ردیف اصلی `SUB` اکنون URL صفحهٔ پروداکشن را کپی/نمایش می‌دهند:

```text
https://YOUR_DOMAIN/sub/UUID
```

این URL هنگام بازشدن در مرورگر، رابط کامل Subscription را نمایش می‌دهد.

## Import در کلاینت‌ها

گزینه‌های Android و iOS در پایین صفحه همچنان URL خام زیر را استفاده می‌کنند تا کلاینت‌های VPN به‌جای HTML، feed استاندارد دریافت کنند:

```text
https://YOUR_DOMAIN/sub/UUID?view=raw
```

بنابراین URL صفحهٔ پروداکشن و URL Import کلاینت‌ها اکنون عمداً جدا هستند و هیچ‌کدام جای دیگری را خراب نمی‌کند.
